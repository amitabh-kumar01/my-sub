import { Admin } from "@/components/pages"
import SimpleLayout from "@/layout/customLayout"
const page = () => {
  return (
    <SimpleLayout>
    <Admin/>
    </SimpleLayout>
  )
}

export default page
